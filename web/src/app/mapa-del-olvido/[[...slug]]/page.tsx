import MapaLoader from "@/components/mapa-loader";

export const metadata = {
  title: "Mapa del Olvido — Cuentas Venezuela",
  description:
    "Mapa interactivo de obras públicas paralizadas, críticas e inoperativas en Venezuela.",
};

export default function MapaPage() {
  return <MapaLoader />;
}
